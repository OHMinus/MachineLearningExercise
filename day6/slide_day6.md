---
title: slide_day6
date: 2026-05-27
---
           2026年度
  
      機械学習演習
  第6回：分類③（決定木／機械学習I に相当）
           計良 宥志
  本日の内容
  1. 分類は領域分割という視点の復習
  2. 決定木：Yes/No ルールによる解釈可能な領域分割
  3. 決定木アルゴリズムの概要
  4. コア問題：2分割の方法と「良い分割」とは何か
  5. 不純度関数：Gini 不純度・エントロピー
  6. 演習：決定木の実装
  
                                  2
  1. 分類と決定木
  
  
              3
  分類
   いい感じの分類境界を引きたい ＝ 領域分割をしたい
   線形2クラス分類 ＝ 領域を2分割
   （一般）多クラス分類 ＝ 領域を多分割（分割数 領域数 であることに注意！）
   複雑な分割 ＝ 解釈性の低下
  
  
  
  
  決定木：複雑だが解釈可能な領域分割を実現                      4
  Akinator
  
  
  
  
             https://jp.akinator.com/
                                        5
  決定木のアイディア
  Idea：分類するための Yes/No ルール（の繰り返し）
  
  
  
  
   要は，2分割を繰り返すことで領域を細かく分けていく．
                                   6
  決定木のアルゴリズム（概要）
  設定： クラス分類．学習データ
  Step 1 をある特徴 に着目し，適当な閾値 で2つに分割する．
  
  Step 2 以降 分割結果 について，（以前と同じとは限らない）ある特徴 と閾値
  で 2 つに分割する．これを繰り返す．
  最終状態 学習データが重複なく分割され，木構造の葉に紐づく．
  
  
                                              7
  決定木による予測
  予測：入力 がどの葉 に属するかを，木をルートから辿って決定する．
    のラベル比率がそのまま のクラス確率になる．
  
  
  
  最後は多数決：              ．
  
  コアな問題：いま があったとき，これをどう2分割するか？
                                      8
  2. 良い分割とは何か
  
  
                9
  コア問題：2分割の方法
  サンプル集合           をどう2分割するか？
  「良い分割」への直感的な要請
   一つの分割（部分集合）には，なるべく同クラスが集まって欲しい．
  これを関数として定式化できれば，分割の良さを数値で比較できる．
  
                                     10
  演習（「良い分割」の具体化）
  サンプル集合                の「良い分割」への直感
   一つの分割には，なるべく同じクラスのものだけがまとまってほしい．
  この直感を実現する関数 を2種類考えてみよ．
  ヒント
   1. そもそも は何の関数になるだろうか？（定義域と値域は？）
   2. まずは2クラス分類（クラス 0 とクラス 1）から考える
   3. の値を縦軸，クラス 0 の割合 を横軸にしたプロットはどんな形になってほしい？
                                                11
  不純度関数（「分割の悪さ」を与える関数）
   は，部分集合 の 「不純度」 （クラスの混ざり具合）を測る関数．2クラス分類でク
  ラス 0 の割合を とすると：
  
  
  
  
       で    ，     で最大 → 凸で対称な関数．
                                              12
  代表的な不純度関数
  部分集合 におけるクラス の割合を                     とする．
  Gini 不純度
  
                         クラス：
  エントロピー（情報量）
                    クラス：
  どちらも**「混ざっているほど大」「片寄っているほど小」**を測る凸対称な関数．
                                               13
  不純度関数を分割に使う
  分割の良さは，分割前の不純度から分割後の重み付き平均をどれだけ減らせるかで測る．
  
  
  決定木の学習アルゴリズム：各ノードで，全特徴 と全閾値 について試し，情報利得
  が最大の分割を選ぶ．
  
  
  停止条件（過学習を防ぐため必要）：ノードのサンプル数下限／木の深さ上限／不純度ゼロ
  など．
                                              14
  3. 演習：決定木の実装
  
  
                 15
  演習：決定木の実装
  雛形 src/decision_tree.py の # TODO を埋めて完成させよ．
  Step 1（データ生成） 2クラス分類用の2次元データを作る．以下のいずれかでよい．
      sklearn.datasets.make_moons / make_circles / make_classification
  
     適当な 2 つのガウス分布からサンプリング
  Step 2（不純度関数） ラベル配列 y から Gini 不純度（またはエントロピー）を返す
   _gini(y) / _entropy(y) を実装せよ．
  
  
  
                                                                         16
  演習（続き）
  Step 3（最良分割） _best_split(X, y) を実装せよ．
   データ           について情報利得が最大となる特徴 と閾値 （および利得）を
   返す．
  
  Step 4（木の構築） _build_tree(X, y, depth) を再帰的に実装せよ．
          を呼んで子ノードを作り，最大深さ self.max_depth 等で停止
   _best_split
   する．戻り値は Node の根． fit から depth=0 で呼ばれる．
                                                     17
  演習（続き）
  Step 5（予測） _predict_one(x) を実装せよ．
   学習済みの木 self.root を根として，            で left ／
   right に降りていき，葉に到達したら label を返す．
  
  
  
  Step 6（可視化）
      学習データの散布図に決定境界を重ねてプロットせよ．
      max_depth を変えて境界がどう変わるか確認（過学習の様子が見えるは
      ず）．
                                                 18
  課題
   演習（決定木の実装）を Moodle で提出（IPython Notebook 形式）．
  発展課題（任意）
                            の結果と比較する．
    sklearn.tree.DecisionTreeClassifier
  
   max_depth を変えて訓練誤差・テスト誤差をプロット（過学習の確認）
                                       ．
   不純度関数を Gini とエントロピーで切り替え，結果に差が出るか比較する．
  
                                                  19
  
